const InitialState = {
    tasks: [],
}

export const taskReducer = (state = InitialState, action) =>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return tasks
    }
}